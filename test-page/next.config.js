/* eslint-disable @typescript-eslint/no-var-requires */

const nextConfig = {
    reactStrictMode: true,
    env: {
        CEFI_BASE_UI: process.env.CEFI_BASE_UI,
    },
}

module.exports = nextConfig
