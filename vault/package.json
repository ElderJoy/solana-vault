{
    "license": "MIT",
    "scripts": {
        "lint:fix": "prettier */*.js \"*/**/*{.js,.ts}\" -w",
        "lint": "prettier */*.js \"*/**/*{.js,.ts}\" --check",
        "sync-page": "cp target/types/vault.ts ../test-page/src/idl/vault.ts && sed -i \"s|export const DEFAULT_VAULT_PROGRAM_ADDRESS = '.*';|export const DEFAULT_VAULT_PROGRAM_ADDRESS = '$(grep -oP '(?<=vault = \")[^\"]*' Anchor.toml)';|\" ../test-page/src/components/common.tsx",
        "deploy": "anchor build && anchor deploy && anchor keys sync && yarn sync-page"
    },
    "dependencies": {
        "@coral-xyz/anchor": "^0.29.0",
        "@solana/spl-token": "0.4.8"
    },
    "devDependencies": {
        "chai": "^4.3.4",
        "mocha": "^9.0.3",
        "ts-mocha": "^10.0.0",
        "@types/bn.js": "^5.1.0",
        "@types/chai": "^4.3.0",
        "@types/mocha": "^9.0.0",
        "typescript": "^4.3.5",
        "prettier": "^2.6.2"
    }
}
